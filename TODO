Todo Items:

  * Break out code into a module
  * TESTS!
  * Calendar interface depends on gcalcli (external application)
  * Luxafor interface depends on a Python script (external application)
  * Figure out a nicer way of monitoring time zone changes
